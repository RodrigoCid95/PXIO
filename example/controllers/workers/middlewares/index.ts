export const midd1: PXIOWorkers.Middleware = ({ data }) => {
  console.log('midd1', data)
}

export const midd2: PXIOWorkers.Middleware = ({ data }) => {
  console.log('midd2', data)
}

export const midd3: PXIOWorkers.Middleware = ({ data }) => {
  console.log('midd3', data)
}

export const midd4: PXIOWorkers.Middleware = ({ data }) => {
  console.log('midd4', data)
}